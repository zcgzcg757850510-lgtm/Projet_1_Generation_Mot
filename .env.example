# 环境变量配置示例
# 复制此文件为 .env 并修改相应值

# Flask 配置
SECRET_KEY=your-very-secret-key-here-change-this-in-production
FLASK_ENV=production

# 服务器配置
HOST=0.0.0.0
PORT=5000

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# 文件上传限制 (字节)
MAX_CONTENT_LENGTH=16777216

# 数据目录配置 (可选，默认使用项目相对路径)
# DATA_DIR=/path/to/data
# OUTPUT_DIR=/path/to/output
